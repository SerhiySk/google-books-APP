import styled from 'styled-components';
const Wrapper = styled.div`
  display: flex;

  span {
    color: #ffb900;

    .text {
      color: var(--grey-800);
    }
  }
  p {
    margin-bottom: 0;
  }
`;
export default Wrapper;
